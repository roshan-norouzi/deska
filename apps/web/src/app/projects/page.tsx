'use client'
import { useState } from 'react'
import { ProtectedLayout } from '@/components/layout/protected-layout'
import { useApi } from '@/hooks/use-api'
import { apiFetch } from '@/lib/utils'
export default function ProjectsPage() { const { data, refetch } = useApi<any[]>('/projects'); const [name,setName]=useState(''); const [description,setDescription]=useState(''); const projects=Array.isArray(data)?data:[]; return <ProtectedLayout><main className="mx-auto max-w-6xl space-y-6 p-6" dir="rtl"><div><h1 className="text-2xl font-bold">مدیریت پروژه و تسک</h1><p className="mt-1 text-sm text-slate-500">پروژه‌ها، اعضا، پیشرفت و تسک‌های سازمان را در یکجا مدیریت کنید.</p></div><section className="rounded-xl border bg-white p-4 shadow-sm"><h2 className="mb-3 font-semibold">ایجاد پروژه جدید</h2><div className="grid gap-3 md:grid-cols-[1fr_2fr_auto]"><input className="rounded-lg border px-3 py-2" placeholder="نام پروژه" value={name} onChange={e=>setName(e.target.value)}/><input className="rounded-lg border px-3 py-2" placeholder="توضیحات پروژه (اختیاری)" value={description} onChange={e=>setDescription(e.target.value)}/><button className="rounded-lg bg-blue-600 px-4 py-2 font-medium text-white hover:bg-blue-700" onClick={async()=>{if(name.trim()){await apiFetch('/projects',{method:'POST',body:{name,description}});setName('');setDescription('');await refetch()}}}>افزودن پروژه</button></div></section><section><h2 className="mb-3 font-semibold">پروژه‌های سازمان</h2>{projects.length===0?<div className="rounded-xl border bg-white p-8 text-center text-slate-500">هنوز پروژه‌ای ایجاد نشده است.</div>:<div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{projects.map(p=><div key={p.id} className="rounded-xl border bg-white p-4 shadow-sm"><div className="flex justify-between"><h3 className="font-semibold">{p.name}</h3><span className="rounded-full bg-emerald-50 px-2 py-1 text-xs text-emerald-700">{p.status==='active'?'فعال':p.status}</span></div><p className="mt-2 min-h-6 text-sm text-slate-500">{p.description||'بدون توضیحات'}</p><div className="mt-4 flex justify-between border-t pt-3 text-sm text-slate-500"><span>{p._count?.tasks??0} تسک</span><a className="text-blue-600" href={`/projects/tasks?projectId=${p.id}`}>مشاهده تسک‌ها</a></div></div>)}</div>}</section></main></ProtectedLayout> }
